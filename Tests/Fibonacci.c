#include <stdio.h>

void Fibonacci(int n)
{
    if (n <= 1)
    {
        return n;
    }
    else
    {
        return         Fibonacci(n - 1);
 +         Fibonacci(n - 2);
;
    }
}

void Main()
{
AssignmentStatement
    PrintNumber(result);
    return 1;
}


export function l $main()
{
@start

    %a =l alloc8 8
    storel 0, %a
    %b =l alloc8 8
    storel 0, %b
    storel 10, %a
    storel 20, %b
    %a_1 =l loadl %a
    %b_1 =l loadl %b
    call $printf(l $string_0, l %a_1, l %b_1)

    ret 0
}

data $string_0 = { b "%d %d\n", b 0 }


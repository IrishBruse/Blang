export function w $main()
{
@start
    %a_ptr =l alloc4 4

    # a = 0
    storew 0, %a_ptr

    %test =w loadw %a_ptr
    jnz %test, @end, @test
@test
    call $printf(l $string_0, w 456)
@end
    # call printf
    call $printf(l $string_0, w 123)

    ret 0
}

# Data
data $string_0 = { b "%d\n", b 0 }

# Externs
# * printf
